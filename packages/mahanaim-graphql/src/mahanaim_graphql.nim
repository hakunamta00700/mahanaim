## GraphQL package skeleton. Transport and schema implementation are intentionally
## deferred until this package owns its independent release/test contract.
const extensionName* = "mahanaim-graphql"

